class Planet extends Base
{
  float distX;
  float distY;
  float distZ;
  
  Planet(float _x, float _y, float _z, float _size, int _colourScheme)
  {
   super(_x, _y, _z, _size, _colourScheme); 
  }
}